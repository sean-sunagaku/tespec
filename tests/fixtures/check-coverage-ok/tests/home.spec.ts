import { expect, test } from '@playwright/test';

test.describe('ホーム画面', () => {
  test('画面を開く', async ({ page }) => {
    // implemented
  });

  test('不正入力を送信する', async ({ page }) => {
    // implemented
  });
});
